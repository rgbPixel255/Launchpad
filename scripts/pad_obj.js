function pad(id, audio, playingColor, pausedColor) {
  this.id = id;
  this.audio = audio;
  this.playingColor = playingColor;
  this.pausedColor = pausedColor;
  /*this.play = function() {
    this.audio.play();
  };*/
}